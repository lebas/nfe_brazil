require "spec_helper"

describe NfeBrazil do
  it "has a version number" do
    expect(NfeBrazil::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
